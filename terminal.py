#Importing Packages
import os
import sys


#Actual code
while True:
        #we need to add a input
        command = input('>> ')
        os.system(command)
        if command == 'exit':
                exit()
